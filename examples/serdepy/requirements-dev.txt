# Tooling for the serialization-norm worked example — the type checker and
# test runner that gate it. Everything the example itself imports is the
# Python standard library; these two are dev-only.
mypy>=1.8
pytest>=8.0
