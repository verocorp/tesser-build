bootstrap.bootstrap imports campaign.client without an alias; a context module is imported as an aliased module, never its members
bootstrap.bootstrap imports linkpolicy.client without an alias; a context module is imported as an aliased module, never its members
bootstrap.bootstrap imports names from campaign.adapters.gateways.target_checker; a context module is imported as an aliased module, never its members
bootstrap.bootstrap imports reports.client without an alias; a context module is imported as an aliased module, never its members
bootstrap.bootstrap never imports tesser.context; a bootstrap module imports tesser.context exactly once, as ts
bootstrap.bootstrap.App is a class; a bootstrap module holds only imports, declared functions, and Final constants
bootstrap.bootstrap.CleanupStack is a class; a bootstrap module holds only imports, declared functions, and Final constants
bootstrap.bootstrap.new is an undeclared module function; a bootstrap function declares itself with @ts.function
bootstrap.config declares a module constant without Final; a bootstrap constant is Final
bootstrap.config imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
bootstrap.config imports names from linkpolicy.wiring.config; a context module is imported as an aliased module, never its members
bootstrap.config imports names from reports.wiring.config; a context module is imported as an aliased module, never its members
bootstrap.config never imports tesser.context; a bootstrap module imports tesser.context exactly once, as ts
bootstrap.config.Config is a class; a bootstrap module holds only imports, declared functions, and Final constants
bootstrap.config.HttpConfig is a class; a bootstrap module holds only imports, declared functions, and Final constants
bootstrap.config._port is an undeclared module function; a bootstrap function declares itself with @ts.function
bootstrap.config.from_env is an undeclared module function; a bootstrap function declares itself with @ts.function
campaign.adapters.gateways.repo_memory imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
campaign.adapters.gateways.target_checker imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
campaign.adapters.gateways.target_checker imports names from linkpolicy.client; a context module is imported as an aliased module, never its members
campaign.adapters.gateways.target_checker imports names from linkpolicy.client; a context module is imported as an aliased module, never its members
campaign.adapters.handlers.cli imports names from campaign.client; a context module is imported as an aliased module, never its members
campaign.adapters.handlers.http imports names from campaign.client; a context module is imported as an aliased module, never its members
campaign.application.parts imports names from campaign.domain.campaign; a context module is imported as an aliased module, never its members
campaign.application.parts imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
campaign.application.service imports errors; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.application.service imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
campaign.application.service imports names from campaign.application.views; a context module is imported as an aliased module, never its members
campaign.application.service imports names from campaign.client; a context module is imported as an aliased module, never its members
campaign.application.service imports names from campaign.domain.campaign; a context module is imported as an aliased module, never its members
campaign.application.service imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
campaign.application.service imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
campaign.application.service imports names from campaign.domain.values; a context module is imported as an aliased module, never its members
campaign.application.service imports secrets; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.application.views imports errors; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.application.views imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
campaign.application.views imports names from campaign.client; a context module is imported as an aliased module, never its members
campaign.application.views imports names from campaign.domain.campaign; a context module is imported as an aliased module, never its members
campaign.application.views imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
campaign.application.views imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
campaign.application.views imports names from campaign.domain.values; a context module is imported as an aliased module, never its members
campaign.domain.campaign imports errors; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.domain.campaign imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
campaign.domain.campaign imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
campaign.domain.campaign imports names from campaign.domain.values; a context module is imported as an aliased module, never its members
campaign.domain.money imports errors; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.domain.money imports serialization; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.domain.short_link imports copy; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.domain.short_link imports names from campaign.domain.values; a context module is imported as an aliased module, never its members
campaign.domain.values imports errors; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.domain.values imports serialization; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.domain.values imports urllib.parse; domain, client, and application import only their context, their tesser package, and the pure stdlib
campaign.wiring.wire imports names from campaign.adapters.gateways.repo_memory; a context module is imported as an aliased module, never its members
campaign.wiring.wire imports names from campaign.application.service; a context module is imported as an aliased module, never its members
campaign.wiring.wire imports names from campaign.client; a context module is imported as an aliased module, never its members
campaign.wiring.wire imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
errors belongs to no governed package; every module belongs to a context, srv, bootstrap, tests, or the protocol package
lifecycle belongs to no governed package; every module belongs to a context, srv, bootstrap, tests, or the protocol package
linkpolicy.adapters.gateways.repo_memory imports names from linkpolicy.application.service; a context module is imported as an aliased module, never its members
linkpolicy.application.service imports names from linkpolicy.client; a context module is imported as an aliased module, never its members
linkpolicy.application.service imports names from linkpolicy.domain.policy; a context module is imported as an aliased module, never its members
linkpolicy.domain.policy imports urllib.parse; domain, client, and application import only their context, their tesser package, and the pure stdlib
linkpolicy.wiring.wire imports names from linkpolicy.adapters.gateways.repo_memory; a context module is imported as an aliased module, never its members
linkpolicy.wiring.wire imports names from linkpolicy.application.service; a context module is imported as an aliased module, never its members
linkpolicy.wiring.wire imports names from linkpolicy.client; a context module is imported as an aliased module, never its members
linkpolicy.wiring.wire imports names from linkpolicy.wiring.config; a context module is imported as an aliased module, never its members
protocol.http declares a module constant without Final; a protocol constant is Final
reports.adapters.gateways.campaign_links imports campaign.client without an alias; a context module is imported as an aliased module, never its members
reports.adapters.gateways.campaign_links imports names from reports.application.service; a context module is imported as an aliased module, never its members
reports.adapters.gateways.policy_verdicts imports linkpolicy.client without an alias; a context module is imported as an aliased module, never its members
reports.adapters.gateways.policy_verdicts imports names from reports.application.service; a context module is imported as an aliased module, never its members
reports.adapters.handlers.http imports names from reports.client; a context module is imported as an aliased module, never its members
reports.application.service imports names from reports.client; a context module is imported as an aliased module, never its members
reports.application.service imports names from reports.domain.report; a context module is imported as an aliased module, never its members
reports.wiring.wire imports campaign.client without an alias; a context module is imported as an aliased module, never its members
reports.wiring.wire imports linkpolicy.client without an alias; a context module is imported as an aliased module, never its members
reports.wiring.wire imports names from reports.adapters.gateways.campaign_links; a context module is imported as an aliased module, never its members
reports.wiring.wire imports names from reports.adapters.gateways.policy_verdicts; a context module is imported as an aliased module, never its members
reports.wiring.wire imports names from reports.application.service; a context module is imported as an aliased module, never its members
reports.wiring.wire imports names from reports.client; a context module is imported as an aliased module, never its members
reports.wiring.wire imports names from reports.wiring.config; a context module is imported as an aliased module, never its members
serialization belongs to no governed package; every module belongs to a context, srv, bootstrap, tests, or the protocol package
srv.cli.main has a loose module-level statement; a srv module holds only imports, declared classes and functions, and Final constants
srv.cli.main imports names from campaign.adapters.handlers.cli; a context module is imported as an aliased module, never its members
srv.http.host imports names from campaign.adapters.handlers.http; a context module is imported as an aliased module, never its members
srv.http.host imports names from reports.adapters.handlers.http; a context module is imported as an aliased module, never its members
srv.http.main has a loose module-level statement; a srv module holds only imports, declared classes and functions, and Final constants
srv.http.main never imports tesser.srv; a srv module imports tesser.srv exactly once, as ts
srv.http.main.main is an undeclared module function; a srv function declares itself with @ts.function
srv.http.router never imports tesser.srv; a srv module imports tesser.srv exactly once, as ts
srv.http.router.Match declares no ts.* base; a srv class declares its block
srv.http.router.Route declares no ts.* base; a srv class declares its block
srv.http.router._path_params is an undeclared module function; a srv function declares itself with @ts.function
srv.http.router.match is an undeclared module function; a srv function declares itself with @ts.function
srv.http.router.split is an undeclared module function; a srv function declares itself with @ts.function
srv.run never imports tesser.srv; a srv module imports tesser.srv exactly once, as ts
srv.run.run_until_signal is an undeclared module function; a srv function declares itself with @ts.function
tests.discovery is neither a test module nor conftest; a tests package holds only test modules and conftest
tests.support is neither a test module nor conftest; a tests package holds only test modules and conftest
tests.test_bootstrap_once imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_bootstrap_once imports names from campaign.client; a context module is imported as an aliased module, never its members
tests.test_bootstrap_once imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
tests.test_bootstrap_once imports names from campaign.wiring.wire; a context module is imported as an aliased module, never its members
tests.test_call_fail_closed imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
tests.test_call_fail_closed imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_call_fail_closed imports names from campaign.client; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.adapters.gateways.repo_memory; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.adapters.handlers.http; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.client; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.domain.campaign; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
tests.test_campaign_lifecycle imports names from campaign.domain.values; a context module is imported as an aliased module, never its members
tests.test_canonical imports names from campaign.domain.campaign; a context module is imported as an aliased module, never its members
tests.test_canonical imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
tests.test_canonical imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
tests.test_cleanup imports campaign.client without an alias; a context module is imported as an aliased module, never its members
tests.test_cleanup imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
tests.test_cleanup imports names from linkpolicy.client; a context module is imported as an aliased module, never its members
tests.test_cleanup imports names from linkpolicy.wiring.config; a context module is imported as an aliased module, never its members
tests.test_cleanup imports names from reports.client; a context module is imported as an aliased module, never its members
tests.test_cleanup imports names from reports.wiring.config; a context module is imported as an aliased module, never its members
tests.test_cleanup imports reports.client without an alias; a context module is imported as an aliased module, never its members
tests.test_cli imports names from campaign.adapters.handlers.cli; a context module is imported as an aliased module, never its members
tests.test_cli imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
tests.test_cli imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_cli imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
tests.test_cli imports names from campaign.wiring.wire; a context module is imported as an aliased module, never its members
tests.test_cli imports names from linkpolicy.wiring.config; a context module is imported as an aliased module, never its members
tests.test_cli imports names from reports.wiring.config; a context module is imported as an aliased module, never its members
tests.test_impl_selection imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
tests.test_impl_selection imports names from campaign.wiring.wire; a context module is imported as an aliased module, never its members
tests.test_impl_selection imports names from linkpolicy.wiring.config; a context module is imported as an aliased module, never its members
tests.test_impl_selection imports names from linkpolicy.wiring.wire; a context module is imported as an aliased module, never its members
tests.test_impl_selection imports names from reports.wiring.config; a context module is imported as an aliased module, never its members
tests.test_reports imports names from campaign.client; a context module is imported as an aliased module, never its members
tests.test_reports imports names from reports.client; a context module is imported as an aliased module, never its members
tests.test_reports imports names from reports.domain.report; a context module is imported as an aliased module, never its members
tests.test_roundtrip_law imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
tests.test_roundtrip_law imports names from campaign.domain.values; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.adapters.gateways.repo_memory; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.adapters.handlers.http; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.application.views; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.domain.campaign; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.domain.money; a context module is imported as an aliased module, never its members
tests.test_serialization_edges imports names from campaign.domain.short_link; a context module is imported as an aliased module, never its members
tests.test_shape imports campaign.client without an alias; a context module is imported as an aliased module, never its members
tests.test_shape imports linkpolicy.client without an alias; a context module is imported as an aliased module, never its members
tests.test_shape imports names from campaign.adapters.handlers.http; a context module is imported as an aliased module, never its members
tests.test_shape imports names from campaign.application.parts; a context module is imported as an aliased module, never its members
tests.test_shape imports names from campaign.application.service; a context module is imported as an aliased module, never its members
tests.test_shape imports names from campaign.client; a context module is imported as an aliased module, never its members
tests.test_shape imports names from campaign.wiring.config; a context module is imported as an aliased module, never its members
tests.test_shape imports names from campaign.wiring.wire; a context module is imported as an aliased module, never its members
tests.test_shape imports names from reports.adapters.handlers.http; a context module is imported as an aliased module, never its members
tests.test_shape imports names from reports.client; a context module is imported as an aliased module, never its members
tests.test_shape imports reports.client without an alias; a context module is imported as an aliased module, never its members
tests.test_shape imports tesser.context; a test module imports only tesser.testing
